{
  "$schema": "http://json-schema.org/schema",
  "$id": "NgSetupSchema",
  "title": "Angular Project Setup Schematic",
  "type": "object",
  "properties": {
    "project": {
      "type": "string",
      "description": "The name of the project to setup",
      "$default": {
        "$source": "projectName"
      }
    },
    "eslint": {
      "type": "boolean",
      "description": "Add ESLint configuration",
      "default": true,
      "x-prompt": "Would you like to add ESLint configuration?"
    },
    "prettier": {
      "type": "boolean",
      "description": "Add Prettier configuration",
      "default": true,
      "x-prompt": "Would you like to add Prettier configuration?"
    },
    "lefthook": {
      "type": "boolean",
      "description": "Add Lefthook git hooks",
      "default": true,
      "x-prompt": "Would you like to add Lefthook git hooks?"
    },
    "vitest": {
      "type": "boolean",
      "description": "Configure Vitest as test runner and remove Karma",
      "default": true,
      "x-prompt": "Would you like to configure Vitest as test runner and remove Karma?"
    }
  },
  "required": ["project"]
}
